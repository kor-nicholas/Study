using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace HelloWorldWebAPI_Cat_
{
    public class ReflectionAndAtributes
    {
        /* Зажать левый CTRL - покажет код класса 
         * 
         * quickWatch (правая кнопка мишы по структуре которую нужно полностью раскрыть для удобного разбора во время дебага)
         * 
         * рефлексия - юзается для пребобразовнаия из одного типа в другой; 
         * DI (dependency injection); 
         * вывод документации в IDEA; 
         * код, который создает свой код и загружает его в клас, котрый потом можно вызвать; 
         * 
         * var myClass = new MyClass(); 
         * myClass.GetType() == typeof(MyClass); 
         */
    }
}
